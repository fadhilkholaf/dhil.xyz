export const metadata = {
    title: "Tailwind Merge Class Names",
    description: "Merging a tailwind utility classes",
    tags: ["Tailwind CSS", "Typescript"],
};

# The Code

```ts
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export const cn = (...inputs: ClassValue[]) => {
    return twMerge(clsx(inputs));
};
```

# Credits

- Explanation in [this article](https://medium.com/@raelsei/how-to-merge-react-and-tailwind-css-class-names-f5faeb10ed24) by [Koray Guler](https://medium.com/@raelsei).
