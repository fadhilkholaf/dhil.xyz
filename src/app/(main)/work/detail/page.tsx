import PageTransitionWrapper from "@/components/wrappers/PageTransitionWrapper";

const page = () => {
  return (
    <PageTransitionWrapper>
      <main className="flex size-full items-center justify-center">detail</main>
    </PageTransitionWrapper>
  );
};

export default page;
